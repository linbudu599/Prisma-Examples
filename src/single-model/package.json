{
  "name": "prisma-with-single-model",
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm generate && ts-node-dev --respawn --transpile-only index.ts",
    "start": "pnpm generate && ts-node-dev --transpile-only index.ts",
    "generate": "prisma db push --force-reset --accept-data-loss"
  },
  "dependencies": {
    "@prisma/client": "^3.8.1",
    "prisma": "^3.8.1"
  }
}
